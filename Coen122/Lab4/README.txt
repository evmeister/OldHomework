README
Evan Eberhardt

Each color on the waveform corresponds to a different buffer.
Each buffer sends its input to corresponding output. Each buffer is connected in a pattern that alternates the inputs with the outputs.