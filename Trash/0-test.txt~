/* 32 keywords */
	auto
	break
	case
	char
	const
	continue
	default
	do
	double
	else
	enum
	extern
	float
	for
	goto
	if
	int
	long
	register
	return
	short
	signed
	sizeof
	static
	struct
	switch
	typedef
	union
	unsigned
	void
	volatile
	while
/* 11 identifiers */
apple
break_
wHile
Cheeeeeeeese
handle While you
_auto
_id
_break
_app13215456asfsad
/* 29 distinct operators */
+ - * / = > < & ! % . , ( ) [ ] { } : ;
== ++ -- -> <= >= != && ||
/* peculiar operator tests */
+++ --- --> === */ /* there should be 10 */